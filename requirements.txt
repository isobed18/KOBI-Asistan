# Core
fastapi>=0.115
uvicorn>=0.34
pydantic-settings>=2.0
python-dotenv>=1.0
PyYAML>=6.0

# AI Agent
langgraph>=0.4
langchain-core>=0.3

# LLM Providers (kullandiginiz provider'a gore yeterli)
langchain-ollama>=0.3          # Ollama (local)
langchain-google-genai>=2.0    # Google Gemini
sentence-transformers>=3.0     # Optional local embeddings for intent similarity
Pillow>=10.0                   # Optional image loading for CLIP visual ingestion

# Scheduler
apscheduler>=3.10

# Telegram
python-telegram-bot>=21.0

# Auth
python-jose[cryptography]>=3.3
bcrypt>=4.0                # passlib yerine doğrudan bcrypt (Python 3.14 uyumlu)
python-multipart>=0.0.9   # OAuth2 form data
